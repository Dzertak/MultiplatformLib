#jdk:
#  - openjdk17
#before_install:
#  - ./scripts/prepareJitpackEnvironment.sh
    # configuration file for building snapshots and releases with jitpack.io
    jdk:
      - openjdk17
    before_install:
      - ./scripts/prepareJitpackEnvironment.sh
    install:
      - FILE="-Dfile=com-multiplatform-lib.aar" #com-atifqamar-customtoast.aar
      - mvn install:install-file $FILE
      - DgroupId=com.github.dzertak
      - DartifactId=com.trackensure.multiplatformlib
      - Dversion=1.0.3
      - Dpackaging=aar
      - DgeneratePom=true
      #com.github.geek-atif                      #com-atifqamar-customtoast  #1.0
